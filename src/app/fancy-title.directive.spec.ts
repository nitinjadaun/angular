import { FancyTitleDirective } from './fancy-title.directive';

describe('FancyTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FancyTitleDirective();
    expect(directive).toBeTruthy();
  });
});
